(function(jQuery, clickContext, eventContext) {function allStorage(){var archive=[],keys=Object.keys(localStorage),i=0,key;for(;key=keys[i];i+=1){let storageName=key;let result=storageName.match(/connext_user_data_/m);if(result){return storageName}archive.push(result)}return false}let getStorageUnit1=allStorage();var authAccount=JSON.parse(localStorage.getItem(getStorageUnit1));authAccount.Subscriptions[0].ExternalSubscriptionId;}(jQuery, clickContext, eventContext))